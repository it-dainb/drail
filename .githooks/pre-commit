#!/usr/bin/env bash
set -e

# Run cargo fmt check — reject commits with unformatted Rust code
if command -v cargo >/dev/null 2>&1; then
    echo "pre-commit: running cargo fmt --check..."
    cargo fmt --check
fi
